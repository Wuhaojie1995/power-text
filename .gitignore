# 环境变量：含 API Key / Token，绝对不能入库。
# 只提交 .env.example 作为模板，真实值每个人本地各自维护。
# 注意顺序：例外规则（!）必须写在前两条之后才生效。
.env
.env.*
!.env.example

node_modules
dist
dist-ssr
power-development
power-production
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Mastra
.mastra

# 工作区里的临时预览产物（动效截图等）：体量大、可随时重新生成，不入库。
# 注意只忽略 preview 子目录 —— .workbuddy-ai/memory/ 是有价值的项目笔记，要保留。
.workbuddy-ai/preview/

# Mastra 本地存储（LibSQL / DuckDB 落盘文件，不入库）
# 实际位置：src/mastra/public/mastra.db 与 src/mastra/public/mastra.duckdb
*.db
*.db-shm
*.db-wal
*.duckdb
*.duckdb.wal

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
